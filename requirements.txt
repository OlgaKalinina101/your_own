# Core API
fastapi>=0.111.0
uvicorn>=0.30.0
python-multipart>=0.0.9

# Settings & config
tzdata>=2024.1
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0,<3.0
alembic>=1.13.0
asyncpg>=0.29.0

# Vector stores
chromadb>=0.5.0

# Embeddings & ML
# torch is ~2 GB and comes in only for one 384-dim embedding model
# (settings.EMBEDDING_MODEL_NAME). Pinned next to it so the cost stays visible.
sentence-transformers>=3.0.0
torch>=2.3.0

# NLP (focus_point extraction)
pymorphy3>=2.0.0
nltk>=3.8.1
# ruwordnet lives in requirements-nodeps.txt: its metadata pins sqlalchemy<2.0,
# which pip cannot reconcile with the 2.0 this project runs on. The pin is stale
# rather than real — see _import_ruwordnet() in memory/focus_point.py.

# Utilities
aiohttp>=3.9.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
# tests/test_lint.py enforces ruff.toml; without it that test skips
ruff>=0.6.0
